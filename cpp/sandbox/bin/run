#!/bin/bash

srcs="${*:-./sandbox.cpp}"

g++ \
  -iquote. \
  -iquote./src \
  -isystem./deps/build/include \
  -std=c++17 \
  -o ./.output \
  "$srcs" \
  -lcrypto \
  -lcurl \
  -lpthread \
  -lssl && ./.output;
\rm -f ./.output
