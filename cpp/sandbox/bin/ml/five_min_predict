#!/bin/bash

app_dir=$(realpath "$(dirname "${BASH_SOURCE[0]}")/../../")

export APP_DIR="${app_dir}"
export PYTHONPATH="${PYTHONPATH}:${app_dir}/src/"

python3 "${app_dir}/src/ml/five_min_predict/main.py" "${@}"
