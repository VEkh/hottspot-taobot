#!/bin/bash

case $1 in
  "generate")
    app_dir=$(realpath "$(dirname "$0")/../../")
    cmd="0 16 * * 0 $app_dir/bin/oanda/start >> $app_dir/log/development.log 2>&1"

    echo -e "$cmd"
  ;;

  "restart")
    sudo service rsyslog restart
    sudo service cron restart
  ;;
esac
