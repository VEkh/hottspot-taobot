#!/bin/bash

app_dir=$(realpath "$(dirname "$0")/../../")

symbol="${1^^}"

if [[ "$*" =~ .*--clear-performance.* ]]
then
  "$app_dir/bin/etrade/clear_performance" "$symbol"
fi

"$app_dir/bin/etrade/stop" "$@"
"$app_dir/bin/etrade/start" "$@"
